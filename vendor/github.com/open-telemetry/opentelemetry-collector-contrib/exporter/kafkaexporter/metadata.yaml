type: kafka

status:
  class: exporter
  stability:
    beta: [traces, metrics, logs]
  distributions: [core, contrib]
  codeowners:
    active: [pavolloffay, MovieStoreGuy]

tests:
  config:
  skip_lifecycle: true
