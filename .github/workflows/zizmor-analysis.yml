name: CI - Zizmor analysis
on:
  push:
  pull_request:

permission: {}

jobs:
  zizmor:
    name: Run zizmor

    permissions:
      actions: read
      contents: read

      pull-requests: write
      security-events: write

    uses: grafana/shared-workflows/.github/workflows/reusable-zizmor.yml@c0944351c68e107649cb9c9a86204b400b607564