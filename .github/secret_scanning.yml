# These catalog fixtures contain deliberately secret-shaped synthetic values
# for detector tests. Each rule records its provenance in evidence.fixture_method.
# Keep production code and all other paths covered by secret scanning.
paths-ignore:
  - "pkg/secrets/testdata/catalog/*.json"
