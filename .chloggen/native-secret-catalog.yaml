change_type: feature
component: tempo
note: Add native secret detection with tenant policies.
issues: []
subtext: "Disabled by default; enable secrets.detection_enabled and the tenant's secret-detection processor. Supports native rules, tenant exclusions, and custom rules. Finding logs never include matched values."
user: carles-grafana
