change_type: change
component: deps
note: Generate Tempo's own protobuf Go code with the wiresmith compiler instead of protoc-gen-gogofaster. The protobuf wire format is unchanged (binary-compatible with the previous gogo output); only the generated Go API and the `make gen-proto` toolchain change.
issues: [7466]
subtext:
user: oleg-kozlyuk
