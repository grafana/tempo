apiVersion: v1
kind: Service
metadata:
  labels:
    name: memcached-parquet-page
  name: memcached-parquet-page
  namespace: tracing
spec:
  clusterIP: None
  ports:
  - name: memcached-client
    port: 11211
    targetPort: 11211
  - name: exporter-http-metrics
    port: 9150
    targetPort: 9150
  selector:
    name: memcached-parquet-page
