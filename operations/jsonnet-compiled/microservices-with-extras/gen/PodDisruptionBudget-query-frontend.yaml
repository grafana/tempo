apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  labels:
    name: query-frontend
  name: query-frontend
  namespace: tracing
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      name: query-frontend
