apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  labels:
    name: memcached
  name: memcached
  namespace: tracing
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      name: memcached
