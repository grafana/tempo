apiVersion: v1
kind: Service
metadata:
  name: rollout-operator
  namespace: tracing
spec:
  ports:
  - name: https
    port: 443
    protocol: TCP
    targetPort: 8443
  selector:
    name: rollout-operator
